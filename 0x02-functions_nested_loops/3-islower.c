#include "main.h"
/**
* _islower  - does my stuff
* @c: The
* Description:does my stuff explained
* Return:Always(Success);
*/
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}

	return (0);
}
